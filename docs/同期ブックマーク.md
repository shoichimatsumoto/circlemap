# CircleMap 同期ブックマーク（コピペ用）

**Vercel の `SYNC_SECRET` は `cmSync235285` に統一**（英数字のみ・URL にそのまま載せられる）。

初回だけ Vercel → Environment Variables → `SYNC_SECRET` = `cmSync235285` → Production → Save → **Redeploy**。

---

## サイちゃん 一括取り込み（maker 235285）

ブラウザのアドレスバーにコピペして Enter:

```
https://www.circlemap.jp/api/sync/maker?makerId=235285&secret=cmSync235285
```

成功例:

```json
{ "ok": true, "message": "サイちゃん の同期完了", "worksSynced": 12 }
```

---

## フォーム版（secret を URL に載せたくないとき）

https://www.circlemap.jp/tools/sync

- maker ID: `235285`
- SYNC_SECRET: `cmSync235285`

---

## 全カタログ同期

```
https://www.circlemap.jp/api/sync?secret=cmSync235285
```

---

## 設定確認（認証不要）

https://www.circlemap.jp/api/sync/check
